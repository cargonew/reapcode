package superPow

func averageWaitingTime(customers [][]int) float64 {    
	var avgTime float64

	waitTimes := make([]int , len(customers))
		
	for i,val := range customers{ 
		curr := val; 
		nextC := customers[i+1]

		waitTimes = append(waitTimes, )



	}
	return  avgTime
}
